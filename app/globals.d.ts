// Local declaration so files inside `app/` can resolve CSS imports immediately.
// This helps editors/tsserver resolve `import './globals.css'` in `app/layout.tsx`.

declare module "*.css";
declare module "*.scss";

export {};
